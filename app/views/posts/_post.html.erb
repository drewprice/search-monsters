<tr id="post-<%= post.id %>">
  <%= render partial: 'post_body', locals: { post: post } %>

  <% if current_user == post.user %>
    <%= render partial: 'post_crud', locals: { post: post } %>
  <% end %>
</tr>

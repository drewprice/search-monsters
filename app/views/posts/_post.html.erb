<tr id="post-<%= post.id %>">
  <% if current_user == post.user %>
    <%= render partial: 'private', locals: { post: post } %>
  <% else %>
    <%= render partial: 'public',  locals: { post: post } %>
  <% end %>
</tr>

<% publish_to '/live-feed' do %>
  $('#posts')
    .prepend('<tr id="post-<%= @post.id %>"><%= j render partial: "public", locals: { post: @post } %></tr>');

  $('#post-<%= @post.id %>')
    .hide()
    .fadeIn();
<% end %>

// TODO: Make it fade in?
$('#post-<%= @post.id %>')
  .replaceWith('<tr id="post-<%= @post.id %>"><%= j render partial: "private", locals: { post: @post } %></tr>');

$('#new_post')[0].reset();

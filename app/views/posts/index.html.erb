<%= subscribe_to '/live-feed' %>

<h1 id="title">Magic Pokémon Stuff?!</h1>

<%= render 'sessions/status' %>

<%= render 'form' if user_signed_in? %>

<table id="posts"><%= render @posts.sort_by(&:created_at).reverse %></table>
